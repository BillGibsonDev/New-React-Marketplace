
import styled from 'styled-components';

export default function CartPage() {
    return (
        <StyledCart>
            im the cart
        </StyledCart>
    );
}

const StyledCart = styled.div`

`;